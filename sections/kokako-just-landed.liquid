{% comment %}
  Kokako "Just Landed" section highlighting new arrivals with optional decorative imagery.
{% endcomment %}
<section id="kokako-just-landed-{{ section.id }}" class="kokako-just-landed section" data-section-id="{{ section.id }}" data-section-type="kokako-just-landed">
  <div class="kokako-just-landed__inner">
    {% if section.settings.decor_image %}
      <img src="{{ section.settings.decor_image | image_url: width: 720 }}" alt="{{ section.settings.decor_image.alt | default: 'Decorative image' | escape }}" class="kokako-just-landed__decor" loading="lazy">
    {% endif %}
    <h2 class="kokako-just-landed__heading">{{ section.settings.land_heading }}</h2>
    <div class="kokako-just-landed__buttons">
      {% for btn in section.blocks %}
        {% if btn.type == 'cta' %}
          <a href="{{ btn.settings.link }}" class="kokako-just-landed__btn" {{ btn.shopify_attributes }}>{{ btn.settings.label }}</a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Just Landed Feature (Kokako)",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "land_heading",
      "label": "Feature Heading",
      "default": "Just Landed, Kōkako Half-Caf Mugs."
    },
    {
      "type": "image_picker",
      "id": "decor_image",
      "label": "Decorative Image (e.g. flying bird)"
    }
  ],
  "blocks": [
    {
      "type": "cta",
      "name": "CTA Link",
      "settings": [
        {
          "type": "url",
          "id": "link",
          "label": "Link URL"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Link Label",
          "default": "Learn more"
        }
      ]
    }
  ],
  "max_blocks": 2,
  "presets": [
    {
      "name": "Just Landed Feature (Kokako)",
      "category": "Custom"
    }
  ],
  "default": {
    "blocks": [
      {
        "type": "cta",
        "settings": {
          "link": "/products/half-caf-mugs",
          "label": "Shop now"
        }
      },
      {
        "type": "cta",
        "settings": {
          "link": "#main-content",
          "label": "I'm just browsing"
        }
      }
    ]
  }
}
{% endschema %}
